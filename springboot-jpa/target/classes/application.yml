server:
  port: 7777
spring:
  application:
    name: springboot-jpa
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springboot?useSSL=false
    username: root
    password: root
  jpa:
    show-sql: true
debug: true
